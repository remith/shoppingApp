// import 'package:bloc/bloc.dart';

// import '../error/failure.dart';

// mixin FailureHandler<S> on Cubit<S> {
//   void handleFailure(Failure failure, S Function(Failure) createFailureState) {
//     final state = createFailureState(failure);
//     emit(state);
//   }
// }
